
extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + " - "}


block content
  include author
  h1= page.title
  p.author
    | #{ "Written by " }
    mixin author(page.metadata.author)
  article.article
    section.content!= typogr(page.html).typogrify()
  a(href='/blog.html')
    a(href='/')
      .btn.btn-default « All blog entries


