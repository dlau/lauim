
extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + " - "}


block content
  .container
    include author
    h1= page.title
    p.author
      | #{ "Written by " }
      mixin author(page.metadata.author)
  .container
    article.article
      section.content!= typogr(page.html).typogrify()
  .container
    a(href='/blog.html')
      span.label.round « All blog entries


